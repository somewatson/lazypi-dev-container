# Container Configuration Guide

You can customize the container behavior via the `.env` file:

- `CONTAINER_PROMPT`: Change the shell prompt prefix displayed in the Zsh shell.
  - **Default**: `[lazypi-docker]`
  - **Example**: `CONTAINER_PROMPT="[my-dev-env] "`
